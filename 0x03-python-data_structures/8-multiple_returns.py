#!/usr/bin/python3

def multiple_returns(sentence):
    def multiple_returns(sentence):
    bab = len(sentence)
    if bab == 0:
        first_char = None
    else:
        first_char = sentence[0]

    current_tuple = (bab, first_char)
    return current_tuple
